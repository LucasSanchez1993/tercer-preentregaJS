class Mascota{
    constructor(nombre, edad, peso,mail){
    this.nombre = nombre,
    this.edad = edad,
    this.peso = peso,
    this.mail = mail, Date;
}
    toString() {
        return this.nombre + "-" + this.edad + "-" + this.peso}
}